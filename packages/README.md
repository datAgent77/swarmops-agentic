# packages/

Shared contracts and utilities reused across `apps/` and `agents/` (e.g. shared
type/schema definitions). Populated as concrete cross-cutting needs appear.

Empty in P00 by design — no premature abstractions.
